clever_age_process:
    configurations:
        test.context:
            entry_point: data
            end_point: data
            tasks:
                data:
                    service: '@CleverAge\ProcessBundle\Task\ConstantOutputTask'
                    options:
                        output: '{{ value }}'
